{
  "buildCommand": "cd apps/mobile && npm install && npx expo export --platform web --output-dir dist",
  "outputDirectory": "apps/mobile/dist",
  "framework": null,
  "cleanUrls": true,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
