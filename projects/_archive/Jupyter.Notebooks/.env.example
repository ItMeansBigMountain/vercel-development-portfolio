# Copy to .env and fill local values. Never commit secrets.
# Offline validation is the default and does not require any secrets.
# Set these only when intentionally running live sandbox API cells.
IEX_CLOUD_API_TOKEN=replace-with-your-iex-sandbox-token
NOTEBOOK_OFFLINE=1
LIVE_IEX_API=0
PORTFOLIO_SIZE=100000
