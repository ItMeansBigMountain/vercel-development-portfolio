Classification: script/archive

The `wutHappened` directory is a merge/source archive for the `stockNews` project. It contains product direction documents, development plans, and reusable code snippets, but no live deployment, scaffold, or active backend/API. Therefore it should be treated as a script/archive rather than a live app or scaffold.
